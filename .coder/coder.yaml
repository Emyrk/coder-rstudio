version: 0.2
workspace:
  type: "kubernetes"
  specs:
    kubernetes:
      # Image should already be registered in your coder's organization.
      # Use the image's repo name.
      image:
        # value: codercom/enterprise-base:ubuntu
        value: docker pull emyrk/coder-rstudio
      container-based-vm:
        value: false
      cpu:
        value: 2
      memory:
        value: 2
      disk:
        value: 20
